#include <stdio.h>
#include <stdlib.h>

int main()
{
  int mum=0,a=1;
  char c;
  while((c=getchar())!='\n'){
  if(a==1)
    {
      if(c!=' ')
        {mum++;
        a=0;}
  }
  else if(c==' ')
  a=1;
  }
  printf("%d",mum);
  return 0;
}
